@model STRANDVENTURE.Models.StudentLoginViewModel
@{
    ViewData["Title"] = "Student Login";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - STRANDVENTURE</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/login-styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/icons.css" asp-append-version="true" />
</head>
<body class="student-login-body">
    <div class="login-container">
        <div class="login-card student-theme">
            <div class="login-header">
                <i class="fas fa-graduation-cap login-icon"></i>
                <h2>Student Portal</h2>
                <p class="login-subtitle">Access your STRANDVENTURE learning portal</p>
            </div>

            <form method="post" class="login-form" id="studentLoginForm">
                @Html.AntiForgeryToken()
                
                @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                {
                    <div class="alert alert-danger" role="alert">
                        <i class="fas fa-exclamation-triangle"></i>
                        @Model.ErrorMessage
                    </div>
                }

                @if (!string.IsNullOrEmpty(Model.SuccessMessage))
                {
                    <div class="alert alert-success" role="alert">
                        <i class="fas fa-check-circle"></i>
                        @Model.SuccessMessage
                    </div>
                }

                <div class="form-group">
                    <label asp-for="Input.LRN" class="form-label">
                        <i class="fas fa-id-card"></i>
                        Learner Reference Number (LRN)
                    </label>
                    <input asp-for="Input.LRN" class="form-control lrn-input" placeholder="Enter your 12-digit LRN" maxlength="12" />
                    <small class="form-text text-muted">Your LRN is a 12-digit unique identifier (e.g., 123456789012)</small>
                    <span asp-validation-for="Input.LRN" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Password" class="form-label">
                        <i class="fas fa-lock"></i>
                        Password
                    </label>
                    <div class="password-input-group">
                        <input asp-for="Input.Password" class="form-control" placeholder="Enter your password" />
                        <button type="button" class="password-toggle" onclick="togglePassword('Input_Password')">
                            <i class="fas fa-eye" id="Input_Password_icon"></i>
                        </button>
                    </div>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.CaptchaInput" class="form-label">
                        <i class="fas fa-shield-alt"></i>
                        Security Verification
                    </label>
                    <div class="captcha-container">
                        <div class="captcha-image-container">
                            <img id="captchaImage" src="data:image/svg+xml;base64,@Model.CaptchaImageBase64" alt="CAPTCHA" />
                            <button type="button" class="captcha-refresh" onclick="refreshCaptcha()" title="Refresh CAPTCHA">
                                <i class="fas fa-sync-alt"></i>
                            </button>
                        </div>
                        <input asp-for="Input.CaptchaInput" class="form-control" placeholder="Enter CAPTCHA code" maxlength="5" />
                    </div>
                    <span asp-validation-for="Input.CaptchaInput" class="text-danger"></span>
                </div>
                @*
                <div class="form-group form-check">
                    <input asp-for="Input.RememberMe" class="form-check-input" />
                    <label asp-for="Input.RememberMe" class="form-check-label">
                        Remember me for 30 days
                    </label>
                </div>
                *@
                <button type="submit" class="btn btn-login student-btn" id="loginBtn">
                    <span class="login-text">
                        <i class="fas fa-sign-in-alt"></i>
                        Sign In
                    </span>
                    <span class="loading-spinner" style="display: none;">
                        <i class="fas fa-spinner fa-spin"></i>
                        Signing In...
                    </span>
                </button>

                <div class="login-links">
                    <a href="/Home/StudentForgotPassword" class="forgot-password-link">
                        <i class="fas fa-key"></i>
                        Forgot Password?
                    </a>
                    <div class="divider">|</div>
                    <a href="/Home/TeacherLogin" class="switch-portal-link">
                        <i class="fas fa-chalkboard-teacher"></i>
                        Teacher Portal
                    </a>
                </div>
            </form>
        </div>

        <div class="login-footer">
            <p>&copy; 2025 STRANDVENTURE - Student Management System</p>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/js/login-script.js" asp-append-version="true"></script>
</body>
</html>